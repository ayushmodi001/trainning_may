import java.util.Scanner;

public class patterns_ltr {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        int space,star,line=1;
        for(line=n;line>=1;line--){
            for(space=1;space<=n-line)
        }
    }
}
